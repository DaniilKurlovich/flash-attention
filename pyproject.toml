[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "flash-attention"
version = "0.1.0"
description = "My reference implementations for Flash Attention"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = [
    "torch>=2.2",
]
